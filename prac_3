code for flutter application to add Text Widget.
import'package:flutter/material.dart';
void main()
{
  runApp(
    const Center(
      child: Text('Hello World',
        textDirection: TextDirection.ltr,
        style: TextStyle(color: Colors.limeAccent,fontSize: 80),
      ),
    )
  );
}


code for flutter application to design Image widget
import 'package:flutter/material.dart';

void main() {
  runApp(
      Image.asset('assets/images/dog.jpg')
  );
}

code  to create a button and display a text on the click of the button.
@override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text(widget.title),
        ),
        body: TextButton(
          child: Text('submit'),
          onPressed: () {
            print('Button is clicked');
          },
        )
    );
  }
} 

